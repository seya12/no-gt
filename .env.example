# Database URL (replace with your Vercel Postgres URL)
# Local development
DATABASE_URL="postgresql://no-gt:no-gt@localhost:5432/no-gt"
# Production (Vercel Postgres URL will be added later)
# DATABASE_URL="postgresql://..."

# NextAuth.js configuration
# In development, this will be http://localhost:3000
# In production, this will be your Vercel domain
NEXTAUTH_URL="http://localhost:3000"
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET="generate_a_secure_random_secret_here"

# GitHub OAuth - Create an OAuth app at https://github.com/settings/developers
# Add both http://localhost:3000/api/auth/callback/github and your production URL as callback URLs
GITHUB_ID="your_github_client_id"
GITHUB_SECRET="your_github_client_secret"

# Environment mode - development, test, or production
NODE_ENV="development"

# Vercel configuration (for deployment)
# VERCEL_URL is automatically provided by Vercel in production
# VERCEL_ENV="preview" 